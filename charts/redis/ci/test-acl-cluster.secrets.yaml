# Companion manifest for ci/test-acl-cluster.yaml.
# Applied by test-charts.sh before the chart is installed for this scenario.
apiVersion: v1
kind: Secret
metadata:
  name: redis-acl-cluster
type: Opaque
stringData:
  users.acl: |
    user default on >ClusterPass123! ~* &* +@all

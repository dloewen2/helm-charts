# Companion manifest for ci/test-acl-standalone.yaml.
# Applied by test-charts.sh before the chart is installed for this scenario.
apiVersion: v1
kind: Secret
metadata:
  name: redis-acl-standalone
type: Opaque
stringData:
  users.acl: |
    user default off
    user redisadmin on >RedisAdminPass123! ~* &* +@all
